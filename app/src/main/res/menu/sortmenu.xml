<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/fileSort"
        android:title="Modified"
        android:icon="@drawable/ic_sort_icon"
        app:showAsAction="ifRoom">
        <!-- "file" submenu -->
        <menu>
            <item
                android:id="@+id/nameSort"
                android:title="Name" />
            <item
                android:id="@+id/modifiedSort"
                android:title="Modified" />
            <item
                android:id="@+id/sizeSort"
                android:title="Size" />
        </menu>
    </item>
    <item
        android:id="@+id/ordering"
        android:icon="@drawable/ic_keyboard_arrow_down_black_24dp"
        android:iconTint="@color/dark_blue"
        android:title="Size"
        app:showAsAction="ifRoom" />
</menu>